import 'package:bt_logic/bt_logic.dart';

class GenerateFieldParam {
  final int size;
  final Difficulty difficulty;
  final int deepCheck;
  final SolverService solver;

  GenerateFieldParam(this.size, this.difficulty, this.deepCheck, this.solver);
}